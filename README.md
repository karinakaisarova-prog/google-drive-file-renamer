# 📂 Google Drive File Renamer (GAS)

Автоматизируйте переименование сотен файлов в Google Drive с помощью одной таблицы. Скрипт идеально подходит для **полной унификации названий** и необходим в случаях, когда требуется постоянно обрабатывать большие массивы документов, приводя их к единому стандарту.

## 📊 Структура таблицы
Для работы скрипта подготовьте Google Таблицу со следующей структурой (строки с названиями файлов идут сразу друг за другом):

| Колонка A (Старое название) | Колонка B (Новое название) |
| :--- | :--- |
| договор_проект_123.pdf | 2024-02-15_Договор_Проект_01 |
| Скан_01.03.2024.docx | 2024-03-01_Счет_Клиент |
| фото_отчет_финал.jpg | 2024-03-10_Фото_Объект_А |
| ... | ... |

## ✨ Особенности
* **Унификация имен:** Позволяет быстро привести файлы с «разношерстными» названиями к строгому корпоративному или личному стандарту.
* **Умный поиск (Fuzzy Match):** Скрипт нормализует имена (убирает знаки препинания, лишние пробелы, меняет регистр), чтобы найти файл, даже если в таблице старое имя указано не совсем точно.
* **Сохранение дат:** Специальная обработка дат формата `дд.мм.гггг`.
* **Проверка расширений:** Автоматически пробует подставлять популярные расширения (`.pdf`, `.docx`, `.xls` и др.), если они не указаны в таблице.
* **Оптимизация:** Использует кэширование списка файлов для быстрой работы с папками любого объема.

## 🛠 Как пользоваться
1. Создайте Google Таблицу. В колонке **A** укажите текущие имена файлов, в колонке **B** — новые (желаемые) имена.
2. Создайте скрипт в меню **Extensions -> Apps Script**.
3. Скопируйте код из файла `renameFiles.gs` этого репозитория в редактор Google Apps Script.
4. Вставьте ссылки на вашу таблицу и папку в объект `CONFIG` в начале кода.
5. Нажмите кнопку **Run**.

## 🤖 Мой личный кейс: ИИ + Унификация
Я использую этот скрипт для поддержания порядка в документах, названия для которых генерирует нейросеть (Gemini). 

**Зачем это нужно:**
Когда файлов много и они приходят из разных источников, их имена — это хаос. Я использую ИИ, чтобы создать «идеальный» реестр имен по заданному шаблону (например, `Проект_Дата_Тип`), а скрипт берет на себя всю черную работу по переименованию.

Благодаря функции **Fuzzy Match**, скрипт игнорирует мелкие опечатки, лишние кавычки или символы, которые могут возникнуть при генерации имен нейросетью, и гарантированно находит нужный файл на диске для его унификации.

## 📝 Требования
* Google аккаунт.
* Доступ к Google Drive и Google Sheets API (запрашивается при первом запуске).

## Лицензия
MIT
