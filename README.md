# Google Drive File Renamer (GAS)

Автоматизируйте переименование сотен файлов в Google Drive с помощью одной таблицы. Скрипт полезен, когда имена файлов в папке лишь частично совпадают с реестром или содержат лишние символы.

## ✨ Особенности
* **Умный поиск (Fuzzy Match):** Скрипт нормализует имена (убирает знаки препинания, лишние пробелы, меняет регистр), чтобы найти файл, даже если в таблице имя написано не идеально.
* **Сохранение дат:** Специальная обработка дат формата `дд.мм.гггг`.
* **Проверка расширений:** Автоматически пробует подставлять популярные расширения (`.pdf`, `.docx` и др.), если они не указаны в таблице.
* **Оптимизация:** Использует кэширование списка файлов для быстрой работы (избегает лишних запросов к API Drive).

## 🛠 Как пользоваться
1.  Создайте Google Таблицу. В первой колонке (A) укажите текущие имена файлов, во второй (B) — новые имена.
2.  Создайте скрипт в меню **Extensions -> Apps Script**.
3.  Скопируйте код из файла `renameFiles.gs` этого репозитория.
4.  Вставьте ссылки на вашу таблицу и папку в объект `CONFIG` в начале кода.
5.  Нажмите кнопку **Run**.

## 📝 Требования
* Google аккаунт.
* Доступ к Google Drive и Google Sheets API (включается автоматически при первом запуске скрипта).

## Лицензия
MIT

## 🤖 Мой личный кейс: ИИ + Автоматизация
Я использую этот скрипт для наведения порядка в документах, названия которых генерирует нейросеть (Gemini).

**Как это работает у меня:**
1. **Генерация названий:** Я даю задание Gemini проанализировать файлы и составить список новых имен по строгому шаблону (например, `Дата_Клиент_ТипДокумента`).
2. **Подготовка данных:** Gemini выдает таблицу, которую я вставляю в Google Sheets. 
3. **Устранение "галлюцинаций" ИИ:** Поскольку ИИ может иногда добавить лишнюю точку, кавычку или ошибиться в регистре, мой скрипт исправляет эти мелочи. Благодаря функции **Fuzzy Match** (умный поиск), скрипт все равно находит нужный файл в папке и дает ему идеальное имя.

Такая связка позволяет мне обрабатывать сотни файлов за считанные минуты, не тратя время на рутину.
